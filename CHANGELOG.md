# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

只记**对使用者有影响**的改动。纯重构、内部整理不列 —— 那些看 git log。

---

## [v1.12.3] - 2026-08-20

> 捆绑 **IvyeaAgent v1.15.2**：跑着的会话立刻进左栏、和亚马逊无关的问题不再被硬套技能、
> 只读档的提示，三件都要 agent 侧的新契约（开跑就落盘、`readonly_blocked`、技能打分修正）。
> 连着更老的 agent 时这三条不生效，界面行为退回改动前 —— 不会报错。

### 新增

- **执行过程铺成一条能读的时间线**（形态对标 DeepSeek Harness）。以前一轮任务只显示一行
  "思考 …最后半句话… 第 15 步"，底下一大片空白 —— 你既不知道刚才那 15 步干了什么，
  也不知道接下来要干什么，只能干等。现在**一件事一行**往下铺：思考、每一次工具调用、
  写了哪个文件、计划更新，一行一件，鼠标停上去看全文，点开看原始参数。
  **每行等高、绝不换行** —— 这是硬约束：会换行的话，流式的思考每来几个字就多一行，
  屏幕上所有文字都会跟着上下跳。
- **执行过程可以一键收起**，收起后只剩一行"执行过程 · 15 步 · 74.0s"。开关是常驻的、
  带文字的（上一版是行尾一个 9px 的小箭头，没人认得出那是开关）。
- **翻上去看历史时，状态跟着你走**。人在底部时时间线就在眼前，不重复显示；一旦往上翻，
  输入框上方会顶出一条状态坞（当前动作 + Agent 排的下一步 + 完成度 + 第几步 + 用时 +
  停止键），点开是完整计划清单。
- **会话标题按内容自动生成**。左栏此前显示你打的第一句话，而第一句常常是"帮我看下这个"
  "继续"，十条会话六条重名，只能一条条点开找。现在一轮结束后由模型起一个说清楚
  「这段对话在做什么」的短标题。**你手动改过的名字永远不会被覆盖**；起名失败就还是
  显示第一句话。
- **历史会话也看得到用时 / 输入 / 输出**。这些数以前只活在跑那一轮的浏览器内存里，
  刷新一下就没了，打开旧会话只剩"几轮几步"。现在整条会话的累计账随存档落盘
  （需要 IvyeaAgent ≥ 本次版本；更早存下的会话没有这笔账，仍然只显示几轮几步）。

### 变更

- **「+」按钮选中图片时，直接给 AI 看，不再当文档塞进知识库**。此前不管选的是什么都走
  知识库，于是"传张截图问这是什么"变成了：模型只拿到一个文件名，然后在磁盘上到处找它、
  找不到，最后说"我看不见这张图"。现在图片和粘贴/拖拽走同一条视觉链路，文档才进知识库；
  一次也可以选多张。
- **思考中的图标换成生长的常春藤**（品牌绿）。原来是一个紫色星号在原地转圈 —— 转圈是
  "进度未知"的通用符号，和这个产品没有关系。
- 首屏标题改为「意念所至，行动随行」，输入框提示语改为「告诉 Ivyea 你想做什么，剩下的
  交给我……」。

### 修复

- **执行过程那一块不再参差不齐**。思考行没有状态点，旧的弹性布局下它后面的每一列都
  整体左移一格，一屏看下来行行错位。现在每行按固定列排（状态 / 图标 / 类型 / 摘要 /
  耗时），所有行的每一列都从同一个位置起。
- **跑着的会话立刻出现在左栏**，不用等这一轮跑完。以前会话只在一轮结束时才落盘，
  于是跑到一半切去别的页面再回来，这段对话就找不回来了 —— 严重时"回到几小时前的
  某个时间点，之后的全部消失"。（需要 IvyeaAgent ≥ 本次版本。）
- **和亚马逊无关的问题不再被硬套上亚马逊技能**。中文分词把"为什么"切成两半，正好撞上
  技能触发词「为什么卖不好」，于是问代码、问报错、问服务器都会先匹配上一本 ASIN 审计
  手册，然后整轮被它带着跑，结尾还要引用知识。（需要 IvyeaAgent ≥ 本次版本。）
- **只读档挡下写操作时，界面直接说清楚**。只读档只分析不改动，**也不会产生待审批项** ——
  以前用户只看到模型转述的一句"被拦截"，然后跑去「待审批」页空等。现在这一轮下面会出现
  一条提示，写明是哪一档挡的、待审批页看不到东西是正常的，并且给一个按钮直接换成
  「审批放行」。
- **任务台里新开的会话不会再"切走一趟就没了"**。以前在空白任务台发第一条指令，会话 id
  只存在内存里，切到别的板块再回来（组件重建）就没有依据恢复会话，落地的是一片空白 ——
  对话其实一个字都没丢，但看起来像那条指令压根没发出去，只能再打一遍。现在第一条消息发出
  去就把会话写进地址栏：刷新、把链接发给别人、以及从侧边栏点回「任务台」，三条路都回到
  同一条会话；侧边栏的「任务台」也会记住刚才开着的那一条。「新建任务」照旧是干净的空白。
- **待审批不再挂着点不动的僵尸卡片**。审批记录此前只有在决策或超时回到本机时才销账，
  而关掉页面、断链、Agent 重启这三种情况下这一步在 Agent 那边早就收摊了 —— 于是会话都
  结束几天了，「待审批」里还留着一张点了只会报错的卡片。现在每次打开这一页都拿 Agent
  那边"真的还在等谁点"的清单对一次账。（问不到 Agent 时不动任何一条：宁可多显示一张
  过期卡片，也不能把真正等着的审批清掉。）
- **待审批的「去看完整上下文」真的跳到那条会话了**。此前它一律跳到空白的任务台，
  等于什么上下文也没看到。
- **回答的最后一句不会再被吞掉**。收尾的那几个字和结束事件常常在同一个网络分片里到达，
  此前收到结束事件会把还没落地的那一帧文字直接丢弃 —— 于是 AI 不回传规范终稿的那几档
  （老版本 IvyeaAgent、兜底通道）里，回答会莫名其妙少最后一段。

## [v1.12.2] - 2026-08-18

> 捆绑 **IvyeaAgent v1.15.1**：审批第三档和上下文进度条都靠 agent 侧的新契约
> （`approval=auto`、`context` 事件）。连着更老的 agent 时，第三档会退回只读、
> 进度条整块不出现 —— 不会画一个编出来的百分比。

### 新增

- **任务台审批改成三档**：**只读** / **审批放行** / **完全放行**。以前只有前两档，
  长任务里"审批放行"意味着每一次写入都要人点一下确认卡，改十几个文件就得守着屏幕点
  十几次。「完全放行」是**一次性授权这一轮**，写操作直接执行。这一档在输入框里是红色
  加闪电图标的芯片，简约皮肤下也保留边框和底色 —— 它必须一眼看得出来。
  智能体预设（能力市场）同步支持第三档。
- **上下文进度条**。输入框下面多了一条细进度条和百分比：这条会话把模型的上下文窗口
  用掉多少了。点开是细账 —— 系统提示词 / 工具 / 对话消息各占多少，以及"窗口快满了，
  新开一轮会更快也更准"的提示。数字是**估算**（细账里写明了），老 agent 不回报这项时
  整块不显示。**打开历史会话就有**（由会话详情带回整份存档的占用），切换会话立刻换成
  那一条的数、新建任务清空 —— 不会把上一条会话的数留在屏幕上冒充当前会话。

### 变更

- **输入框里的「自动选技能」下拉去掉了**。技能本来就按对话自动匹配，那个下拉的默认值
  就叫"自动选技能"，手动挑一个反而是把自动匹配**关掉** —— 一个越用越错的控件。
  需要钉死某个技能时用 `/` 命令，定时任务那边的技能选择不受影响。
- **会话细账默认摊开，而且永远只占一行**。以前默认只露两项（几轮几步 + 用时），
  其余六项藏在「细账」按钮后面 —— 一轮跑完想知道钱花在哪还得再点一下。现在整行
  （上下文进度条 + 轮次 / 用时 / LLM 耗时 / 首字 / 速度 / 缓存命中 / token）
  一次摊开，**装不下就缩字号，绝不换行**：折行会把输入框整块往上顶，页面在跑任务的
  过程中自己跳一下。窄到手机竖屏时缩到下限还装不下，那一行可以横向滑动，一项都不丢。
- **输入框底栏和统计条的字号重排**。芯片（工作区 / 审批档位）此前和模型名"同一档"
  —— 因为「静谧」「门道」这两套主题把全局的 17 档字号压成 4 档，`--fs-105` 和 `--fs-10`
  落进同一个 14.5px，紧挨着 16px 的正文，整条底栏比输入区本身还抢眼。现在底栏按
  **相对正文的比例**取字号（芯片 .82em、统计条与进度条 .76em），四套刻度下都成立：
  静谧主题 16 / 13.1 / 12.2px，经典主题几乎逐像素不变。统计条与进度条并成一行、
  **随内容长短居中**在输入框下方。
- **输入框底栏重排**：左边是"这一轮怎么跑"（附件 / 工作区 / 审批档位 / 人设），
  右边是"用谁跑、发不发"（模型 / 发送）。芯片改成无框式、加了图标，发送键换成
  实心圆形箭头。芯片一多就换行、把发送键顶到第二行的老毛病一并修掉（手机上仍然折行，
  但会先把模型名收成图标，绝不裁掉审批档位）。
- **芯片下拉浮层重做**：选项标题和说明分两行、说明整句显示、当前档位带对勾。
  以前说明和标题挤在一行且不换行，最该看清的那半句被裁成「Agent 只分析和给方案，绝不改…」。

### 修复

- **「系统配置」弹窗里的字体选择点了没反应**。下拉浮层是 portal 到 `<body>` 的
  （不这样会被 overflow 祖先裁掉），于是它和弹窗是同级兄弟、谁在上面只看 z-index ——
  浮层 300、弹窗 9000，浮层**画在弹窗背面**：箭头翻成了 ▴、`elementFromPoint` 却打在
  弹窗的卡片上。现在浮层（含手机端抽屉）压过一切对话框。同一只弹窗里的令牌有效期等
  下拉一并修好。
- **侧边栏「全部工具」展开后，会话列表整块消失、工具树自己也滚不动**。上一版侧栏分成
  固定区/会话区/收纳区三段，而「全部工具」一展开固定区就长到 949px —— 比整条导航
  （791px）还高，会话区被压成 0 高。现在**只冻结「新建任务」这一栏**，一级项、全部工具、
  会话、我的工具都在同一条滚动里（对标 DeepSeek Harness 的侧栏）。
- 顺带修掉"收起「全部工具」时只有下半截能滚"—— 同一条侧栏两种滚动行为。

---

## [v1.12.1] - 2026-08-18

> 离线包捆绑 **IvyeaAgent v1.15.0**：那一版把"一句「测试」跑 2 分 16 秒"的老毛病
> 根治了，并按请求性质分了三条执行路线，闲聊类问题现在 2~5 秒内答完。

### 修复

- **输入框越打越大**。首屏那只输入框每敲一个字就长一截（实测 106→132→185px，
  最后顶到 200 上限）。自适应高度那段有个正反馈：`scrollHeight` 取的是
  `max(内容, 盒子内高)`，而首屏那只有 `min-height`，量出来永远从下限起步；
  代码又拿这个高度反推 `rows`，rows 抬高固有高度、下次量得更大。现在量之前先摘掉
  下限（量的是内容），rows 恒为 1。
- **「新建任务」「全部工具」整行都能点了**。`<button>` 在 Chrome 里即使
  `display:flex` 也按 fit-content 量宽（同类的 `<a>` 是铺满的），于是这两项**只有
  文字那一小块**可点，右边一大截是死区；「15」这个数量也因此贴在标题后面而不是行尾。
- **一段话滚到输入框底下不再被齐刷刷切一刀**（渐隐高度不足一行）。
- **新会话立刻出现在左栏**，不用刷新整页。以前只在**整轮跑完**时才刷新一次，
  而一轮动辄几分钟。
- **一次贴多张图时，刚贴的那张可能被当成"最旧的"清掉**，随后 agent 一调作图就报
  「附图引用已过期或不存在」。图落盘时的文件名 = 毫秒时间戳 + 随机尾巴，清理按文件名
  排序 —— 而一次贴 6 张在快机器上会全落进同一毫秒，前缀完全相同，排序就退回到那条
  随机尾巴。实测旧实现 40 次里有 **27 次**把最后贴的那张删掉。现在时间戳后面加了
  进程内自增序号，同一毫秒内也有严格先后。
- **深色主题下站标不再是一块白方块**（侧栏、左下角头像、首屏三处）。站标 PNG
  本身是不透明白底，现已抠成透明；浅色主题下逐像素零变化。

### 变更

- **任务台首屏重做**（对标 DeepSeek Harness / ChatGPT 的首屏版式）：输入框放大成
  整屏主角、加一团很淡的氛围光晕、场景入口从两列网格改成居中一行（原来 5 个入口
  必然剩一个落单挂在左下角）。
- **正文和侧栏的文字压深一档**。次级/三级文字原来是 `110 / 156`，其中 156 在纯白上
  只有 2.8:1、连 AA 都不到，而侧栏会话标题、活动行、统计条全挂在这两档上，整屏
  读起来是一片灰。现在 88 / 130（6.9:1 / 4.0:1），会话标题更进一步改用正文色。
- **执行过程那一行清理干净**：去掉与它完全重复的「正在处理 · 用时」行（同一个秒数
  说了两遍，两行还差 11px 缩进）；前面原本"绿色 ⟳ 在转 + 一枚 ✻"两枚图标并排、
  转的那枚跟它描述的事情无关，现在只留一枚 —— 跑的时候转的就是当前这件事自己的
  图标（思考是紫色 ✻），收尾才变 ✓。
- **输入框下方的统计条**默认只留「几轮几步 · 用时」，其余（LLM / 首字 / tok每秒 /
  缓存命中 / 输入输出）收进「细账」点开看；整条跟着输入框居中。老 agent 一项都不
  回报时，它原来会排出六个「—」钉在输入框底下。
- **正文段间距、会话行密度、左下角账户区高度**各松/收一档。
- 侧栏「全部工具」展开后末尾那条「在浮层里打开 ⌘K」删掉 —— ⌘K 和收起态点它开浮层
  这两条路都还在，它是重复的第三个入口。

---

## [v1.12.0] - 2026-08-18

> 本版需要 **IvyeaAgent v1.14.1+**（任务台指挥领星、以及"一句话的活不再被判成复杂任务"
> 都靠它）。离线包已捆绑对应版本。

### 变更

- **Agent 找工具不再兜圈子**。两个坑：领星那 9 个工具注册在 `admin` 模块下，
  Agent 按 `module=lingxing` 查是空的；工具搜索是**整串**子串匹配，
  Agent 打「广告 预算 领星 campaign budget」这种一串关键词必定返回空。
  实测它因此连着换了 5 种问法、调了 7 次工具清单才找到入口。
  现在模块归到 `lingxing`（权限边界不变，非管理员照样看不到），搜索改成按词匹配。
- **在任务台直接指挥领星**。Agent 新增三个工具：`lingxing_datasets`（有哪些数据）、
  `lingxing_read`（读任意数据集：广告活动 / 关键词 / 搜索词报表 / FBA 库存 / ASIN 利润…）、
  `lingxing_operate`（改预算 / 竞价 / 加词 / 否词）。以前它只拿得到"大盘"和"规则引擎候选"
  两个做好的结论，你问"UK 站上周哪个词最费钱"它连数都取不到。
- **新增「Agent 执行档位」**（系统配置 → 领星 → 总开关）：
  - **逐项确认**（默认）：Agent 只建工单，你点了才执行。
  - **自主执行**：Agent 直接改完，护栏、快照、审计、回滚一样不少。
  这个档位对应的设置以前**只在状态里显示、从来没被执行过** —— 界面写着"需要人工确认"，
  实际 Agent 传个参数就能执行。现在它是真的，并且有测试守着「没配过时默认要人确认」。

- **整套界面的排版尺度上调一档**。和 ChatGPT / DeepSeek Harness 并排一比，原来的正文
  10.5~12px 明显偏小、久看吃力，那两家的正文都在 14~16px。门道两套从 10.5/12/13.5/15
  抬到 12/13.5/15/17，静谧两套从 12/13/15/17 抬到 13/14.5/16/18.5。
- **强调色换成品牌绿 `#16a34a`**（logo 里那支嫩芽的颜色）。原来绑的是通用信息蓝，
  于是整套界面的"可点 / 选中 / 进行中"全是蓝的 —— 那不是这个产品的颜色。
  语义蓝仍然是蓝的，两者不混用；深色主题用亮一档的绿保证对比度。
- **「首页」板块改名「运营驾驶舱」**。
- **修 Listing 工作台在手机上步骤条文字重叠**。四个步骤按钮写死 `height:32px` +
  `line-height:1`，手机上一格只有约 84px，「① 素材与洞察」放不下就换行，
  而固定高度会让换出去的那行**直接溢出按钮盒子、压在相邻的字上**。现在放开高度、
  给正常行高、字号降一档，四步仍然一屏可见；页面副标题在手机上收成两行。
- **手机端跟着一起改**：抽屉从 196 加宽到 272（字号上调之后 196 装不下导航项），
  隐藏位移改成 `-100%` 而不是写死的 `-200px`（宽度和位移脱节时，抽屉收起会露出一条边）。
- **侧边栏重做层次**：默认宽度 196 → 264（会话标题基本不再截断）；左上角品牌加大加粗、
  收字距、去掉那条分隔线；导航项从"6px 密排 + 左边一条竖线"改成"圆角整块底 + 更重的字"。
- **「全部工具」移到「定时任务」下面，做成可展开折叠的分组**。它是导航的一部分，
  原来沉在侧栏最底下和账户挤在一起。收起侧栏时点它仍然开浮层。
- **删掉所有页面顶部那行面包屑**（`~/任务台`、`~/能力市场`…）。左边侧栏的高亮项已经
  回答了"我在哪"，再写一遍是重复，还平白占掉 40px。这一条现在只在**真有内容时**才出现
  （板块挂了动作，比如服务器终端的工具条），否则整条消失。
- **任务台首屏重排**：标题 26 → 34px 并收紧字距，输入框加宽到 780px、整体上移，
  场景芯片退成弱提示 —— 原来三样东西在抢同一个焦点。
- **对话页右侧的产物栏只在有产物时出现**。原来六个图标常驻，空会话里就是右边一竖排
  点不出内容的按钮；未打开时也不再画那条左边框。

### 变更

- **左下角的「系统配置」和「使用手册」改成弹窗**，不再整页跳转。这两样都不是"去了另一个
  地方"，而是"在当前工作上方看一眼" —— 跳页会让人丢掉上下文，看完还得想办法回来。
  弹窗左边是目录（系统配置的目录按页面实际分区自动生成）、右边是内容，Esc 或点背景关闭。
  花费芯片、任务台的模型行、能力市场的「去填密钥」也一并改成开弹窗，同一件事只有一种行为。
- **服务器终端页重排**：这一页的动作挂进顶栏，终端列表瘦身收窄。
  终端画面从占视口 **57% 面积变成 65%**（1391×595 → 1455×648）。
  - 顶栏原来只有一行「~/服务器终端」空着 46px，页面自己又画一行把同样的名字写第二遍
    再排按钮 —— 现在合成一行。
  - **终端列表收进顶栏的汉堡下拉**（☰ 当前终端 ▾），页面里不再占一列 ——
    它是"切换到哪个终端"用的，一天点几次，却常年占着整整一列。按钮上直接显示当前终端，
    不打开也知道自己在哪。终端画面因此到 **1659×648（占视口 74%）**。
  - **终端列表项从卡片压成一行**（177px → 33px）。一张卡原来四行，只有「名字 + 在不在跑」
    有用：状态徽标下面又写一遍「服务 active / running」、一段每次都一样的固定说明、
    还有三个和主区头部完全重复的按钮；会话卡则是几乎永远是 /root 的工作目录、
    「[terminal closed] reason=shutdown」这类噪音、和占一整行的完整日期时间。
    现在是「状态点 + 名字 + 状态词」，其余进悬停提示；这一列也从 260px 收到 196px。
  - 终端头部两行压成一行（标题和状态并排）。
  - 移动端不变：那边终端列表本来就是底部抽屉，不是常驻列。

- **知识库工作台大幅精简**：七个标签变四个，按"来这一页要干什么"排 ——
  **搜索**（找一条知识，最高频）→ **页面**（看/改）→ **上传**（新增）→ **治理中心**（审核·覆盖·时效·冲突）。
  - **「对话」标签删了**：它和任务台是同一个引擎、同样注入知识检索，却少了工具、审批、
    步骤时间线和引证渲染。想问知识库在任务台问就行，这一页专心管知识本身。
  - 「概览」那四个统计数字并进页头一行（知识卡数 / 分类数 / 引擎状态），看一眼不值得切标签；
    「设置」收进右上角齿轮。
- **右下角悬浮球只剩对话**。它的「知识库」「状态」两个标签分别重复了知识库工作台和系统配置 ——
  同一件事两个入口，两边还会各自演化。保留它唯一不可替代的那点：在任何板块上不离开当前
  工作就能问一句。

- **左上角改叫「IvyeaOps」**（原来是「个人工作台」）。
- **侧边栏会话列表的时间改成日期数字**：今天的显示时:分，今年的显示月/日，跨年补年份，
  并调淡一档。原来那句「3 小时前 / 2 天前」在一屏几十条时和会话标题抢注意力，
  而它本来只是排序的副产品。
- **「Skill 中心」并入「能力市场」的「技能」标签**，侧边栏里不再单列。
  这两个板块给人的感觉一直是重复的 —— 而真正重复的是**同一批技能被列了三遍**：能力市场的
  只读卡片、Skill 中心的可运行列表、Skill 中心的文件管理，还分在两个板块里。
  - 现在只有一份列表，运行 / 创建 / 管理是同一批技能上的三个动作，用分段控件切换。
  - Agent 内置技能和你自己的技能列在同一屏，按来源给不同动作：内置的「在任务台用」，
    自己的「运行 / 编辑」。
  - 老书签 `/skill-hub` 会自动跳转，连深链一起平移（`?tab=create` → `?tab=skills&seg=create`）。
  - 权限没有变化：「Skill 中心」这项授权仍然存在（显示名改为「技能（运行 / 创建 / 管理）」），
    没有这项权限的成员只看得到 Agent 内置技能。

- **知识库工作台偶发「页面渲染出错」的加固**。接口少给一个字段（`files`/`uploads`/`items`），
  下面的 `.length`/`.map` 就会抛，而 React 的错误边界会把**整页**换成报错、只能刷新 ——
  一个字段缺失不该让整页消失。这几处已改成缺字段时按空列表处理。
- **四个页面对"接口少给字段"更耐受了**（分析工具 / 资讯 / 服务器监控 / 用户管理）。
  和知识库那次同因：拿到的不是数组就直接 `.map`/`.filter`，一炸整页被换成「页面渲染出错」。
  返回数组的接口已在 API 层统一收口 —— 新页面天然就是安全的。
- **前端崩溃现在会报到服务端日志**。错误边界原来只 `console.error`，那条信息只存在于
  用户的浏览器里，维护者这边什么都看不到，只能靠猜。现在崩溃会带上是哪一页、什么报错、
  组件栈落进服务端日志（`journalctl -u ivyea-ops`，搜「前端崩溃」）。
- **右下角悬浮球在浅色主题下不再是一整块黑的**。它自己维护了一套写死的深色调色板，
  只给 `[data-theme="light"]` 配了浅色覆盖 —— 而主题表里有**三个**浅色主题
  （静谧·浅 / 门道·浅 / 月岩），那条覆盖只命中最后一个。现在直接用主题变量，
  16 套主题自动都对。

### 修复

- **账户菜单点「字体与字号」现在真的能到外观设置**。以前点了像是"跳到系统配置首页就没下文"，
  真正的原因是：外观区被折叠在「系统状态与更多设置」里，**元素压根还没渲染出来**。
  现在深链会自动展开那个折叠块，并滚到位置稳定为止（这一页的内容是异步来的，
  滚过去之后上面的分区还会继续变高、把目标一路往下推）。

### 移除

- **终端页的「会话内容快照」没了**，连同后台每 5 分钟一次的自动采集。终端会话的留存归
  「外部智能体」板块管，这里再存一份 tmux 面板截图既重复又没人看。
  终端本身**铺满了空出来的宽度**（原来那一栏占 360px）；收起终端列表时终端占满整行。
  **已经存下来的数据没有删** —— 只是不再产生新的，也不再有界面读它。

---

## [v1.11.0] - 2026-08-17

> 本版需要 **IvyeaAgent v1.14.0+**（技能库挂载靠它）。离线包已捆绑对应版本。

### 新增

- **任务台的回答里能直接看到图了**。以前 Agent 出的图只会显示成一串裸地址 —— 正文渲染器不认
  markdown 图片和链接。现在图就显示在回答里，链接也能点。导出的 HTML 报告同样保留图片。
- **贴一张图，说怎么改，就是在你这张图上改**。附图会被交给作图链路当原图（真·图生图，保留原图
  内容），不再只是被视觉模型读成文字。点回答里的任意一张图，就把它收进输入框当下一轮的原图，
  可以一轮轮改下去。
- **IvyeaAgent 没起来时，任务台还能纯聊**。自动退回「全局兜底大模型」那条多 provider 链，
  时间线上会明确标出这一轮走的是备用通道（无工具、不进会话库）。

- **Skill 中心的 amazon 技能现在能被任务台自动匹配到了**。以前这台机器上有两套互不相通的
  技能库：Skill 中心里建的技能，任务台怎么问都匹配不到 —— 界面上还没有任何地方解释这件事。
  现在技能库**原地挂**给 IvyeaAgent（需 IvyeaAgent v1.14+），说需求时自动命中，
  连技能里的脚本和参考文档它也读得到。**改完立即生效，没有同步这一步。**
  Skill 中心「管理」页新增一条状态，显示挂了几个、必要时可重新挂载。
  - 只挂 amazon 分类：全库近百个技能里有一堆和亚马逊运营无关的，全挂上去只会淹掉匹配。
    其余技能照旧在 Skill 中心「工具」页手动运行。
  - **仍然是只读的**：技能里的脚本是给模型读的参考资料，不是执行许可。
  - 技能作者可以在 SKILL.md 的 frontmatter 里写 `triggers: [搜索词, 否词]` 指定什么话
    该命中这个技能。**写成短词**，别写成「调研报告、选品调研、市场分析」这样的整串
    或者「帮我分析这个类目」这种口语 —— 那样会把不相干的提问也拉进来。

### 修复

- **能力市场不再对没有权限的用户显示一个必然报错的首屏**。「社区市场」需要 Skill 中心权限，
  而没有这个权限的成员打开能力市场，第一屏拿到的是「连不上门道社区」—— 明明是权限不够，
  看着像社区挂了。现在这一格连同「Skill 中心技能」区块会直接隐藏，落到「技能」页。
- **能力市场里的技能库路径说明写错了**。标着 `~/.hermes/skills`，实际早就迁到工作台数据
  目录下了，照着那行字去找文件会扑空。

### 移除

- **「AI 问答」和「AI 生图」两个板块并入任务台**，侧边栏里不再单列。
  - AI 问答本来就是任务台同一个 agent、同一个会话库，只是不带工具那一档 —— 直接在任务台问即可。
  - 作图说一句「画一张主图」就行，任务台会自动调同一条生图链路（模型/Key 仍在「系统配置 →
    图片生成服务」里配，没有变）。
  - **历史对话一条没丢**：过去在 AI 问答里的会话仍在任务台左栏，可按「AI 问答」来源筛选、
    点开继续聊。老书签 `/assistant`、`/imagegen` 会自动跳到任务台。

## [v1.10.1] - 2026-08-16

接上一版把 GBrain 摘干净之后的收尾，清掉它留下的最后几处痕迹。

### 移除

- **系统状态里的「兼容 · Ollama Embedding」没了**。那一行和它的安装按钮是给 GBrain 的语义检索用的（按钮拉的就是 `ollama pull nomic-embed-text`，400MB+），而 IvyeaAgent 的语义检索是随包自带的，装完即用、不需要任何服务。
  - 知识库页面那条「前往系统配置 → 智能体 → 知识库语义检索 → 选择服务商（Ollama 本地免费）」的提示同时删掉 —— **它指向的配置区在 v1.8.0 就已经不存在了**，点过去什么都没有。
  - 仍然可以把 Ollama 当**本地大模型**用（首启向导里那条「装 Ollama 之后提供商选自定义」照常有效），那和 embedding 是两回事。

### 变更

- **IvyeaAgent 离线时，知识库会明确告诉你「未连接」**，而不是悄悄返回空结果。以前还有个 GBrain 兜底，现在知识库只有 IvyeaAgent 一个后端 —— 返回空会让你以为库里根本没这些内容。
- 首次进入知识库的引导不再教你「配置 GBrain CLI、知识库根目录和 Embedding 模型」，这三样现在一个都不用配。

## [v1.10.0] - 2026-08-16

### 新增

- **孤儿进程告警**（本条为补记 —— 功能随本版发布，当时漏写）。CPU 看门狗此前只盯 IvyeaOps
  自己的进程，机器上跑飞的**其它**程序它一概看不见。现在会额外扫描「脱离了原会话（PPID=1）、
  又不归任何 systemd 服务管、且持续吃 CPU」的进程，默认持续 10 分钟 ≥50% 才告警，走已有的
  通知渠道。

  这条是被真事逼出来的：一个本该几毫秒就退出的一次性命令卡死空转了 8 天半，一直占满一个核，
  全程零告警，最后是人觉得"机器怎么这么卡"才发现的。

  **只报告，不自动杀** —— 告警里给出精确 PID，并明确提示不要用宽泛的 `pkill -f`（误杀一个
  正经进程比多烧几个 CPU 严重得多）。阈值和持续时长可在系统配置里调（`orphan_threshold` /
  `orphan_sustain`）。非 Linux 自动跳过。

### 变更

- **知识库不再需要 GBrain**。`/brain` 现在只靠 IvyeaAgent 就能完整工作——概览、统计、体检、搜索、页面、文件、上传、对话引用，全部走内置的治理知识库。装了 GBrain 的老机器仍可作回退，不装也毫无影响，可以直接删掉 `~/.bun` 和 GBrain 源码目录（约 450MB）。
  - v1.8.0 摘掉的是「必装 + 自动初始化」，**代码依赖一直还在**：13 个函数里有 8 个要启动外部 gbrain 二进制。
  - 最影响体验的一处是**对话引用检索**：它此前完全没有走 IvyeaAgent，导致每次对话要查两次知识库（一次外部二进制、一次 agent 自己），而外部那次的结果基本只用来显示界面上的「引用」。现在合并成一次，用的是语义 + 关键词双路召回，引用质量比原来的纯关键词检索更准。

### 修复

- **知识库页面「找不到」不再触发外部调用**。打开一个不存在的页面时，系统会错把「这张卡不存在」当成「服务出故障」，于是去启动已经不用的 GBrain。现在直接返回「页面不存在」。
- **历史知识卡按分类筛选不再为空**。从 GBrain 导入的卡片在新知识库里统一标成 `legacy_gbrain`，按分类过滤时会把它们**全部**滤掉——而那恰恰是知识库里内容最多的一批。现在会还原它们原本的分类。

## [v1.9.1] - 2026-08-16

### 修复

- **点「更新」拿到的 IvyeaAgent 现在是你看到的那个版本**。此前几个问题叠在一起，导致"提示有新版本"和"实际装到什么"是两回事：
  - 更新和安装一律装 `main` 分支，而"有新版本"的提示比的是 GitHub 最新 **release**。于是提示说更新到 v1.13.0、实际装的是含未发布代码的 main。现在统一装 release tag（安装脚本、面板更新按钮都是）；取不到 release 时**更新直接中止**而不是悄悄退回 main，安装脚本可以退但会明确告诉你"用的是未发布代码"。
  - 升级时跳过了依赖安装（`--no-deps`），版本之间新增的依赖装不上——v1.13.0 新增的本地视觉就属于这种，升级"成功"但功能装完即坏，还坏得很安静。现在正常装依赖。
  - 仍可用 `IVYEA_AGENT_REF=vX.Y.Z` 指定版本（回滚或装分支自测）。
- **Windows 安装脚本加了语法门禁**。`.ps1` 此前没有任何 CI 检查，改坏了只有用户装的时候才会发现——而那时他连 IvyeaOps 都还没跑起来，报错也回不到我们这里。
- **修好 `setup.ps1`（Windows Docker 一键部署）**。它从 2026-05-27 起就有语法错误、**根本跑不起来**：写 `.env` 那行的引号被改坏成了 `"ADMIN_PASSWORD=*** | "PORT=8080"`，PowerShell 直接解析失败。上面那条新门禁第一次跑就把它抓出来了。

## [v1.9.0] - 2026-08-16

### 新增

- **视觉能力改成三档降级，主脑不支持图片不再等于功能没了**。此前判 IvyeaAgent 能不能接带图任务，只看**主脑**有没有视觉能力；于是主脑一配 DeepSeek 这类纯文本模型，Listing 的图片分析就整块空转——产品视觉身份分析直接回退兜底、竞品套图版式逆向直接返回空，界面上一句提示都没有，看着像功能坏了。现在按实际能力分三档（需 IvyeaAgent v1.13.0+）：

  | 档 | 触发条件 | Listing 能做什么 |
  |---|---|---|
  | **T1 主脑直读** | 主脑自带视觉（`gpt-4o` / `claude` / `gemini` / `*-VL` 等） | 全部 |
  | **T2 视觉旁路** | 主脑不支持图片，但配了「视觉复核模型」 | 全部 |
  | **T3 本地量化** | 两者都没配 | 合规 / 比例 / 主体占比 / 配色 / 图上文字**照常分析**；版式逆向与审美判断跳过并明确告知 |

  - **T3 不是「坏了」**：图片在本机被 CV + OCR 量化成读数（尺寸、白底合规、主体占比与偏移、主色板、OCR 文字及其版面分布、套图查重）后交给文本模型判断，**图片不出网**。主图带促销文字、白底不合规、主体过小这类最常见的问题，T3 都查得出来。
  - **T3 查不了的会说出来**：竞品套图版式逆向这类必须真看懂画面的分析，会在方案里标注「本项已跳过」并写明原因和解法，不产出编出来的假版式。
  - 「系统配置 → 系统状态 → AI · 视觉识别」新增档位徽标（T1/T2/T3），直接告诉你当前在哪一档、这一档少了什么。
  - 想从 T3 升到 T2：在「**视觉复核模型**」区块配一个视觉模型即可（硅基流动免费档的 `Qwen/Qwen3-VL-30B-A3B-Instruct` 就够）。**这个配置会自动下推给 IvyeaAgent**，网页和命令行共用同一个视觉模型，不用配两遍。

### 修复

- **套图配色不再塌成三种颜色**。生成方案时模型在「给我五个配色」这类位置容易吐重复值（实测出过 `[白, 白, 红, 红, 黑]`），导致生图的背景 / 承载面 / 辅助 / 强调 / 深中性五个槽位实际只剩三色，整套图没了层次。现在统一去重。
- **视觉链顺序修正**。IvyeaAgent 在文本链里恒排第一，但视觉链照抄这条规矩是错的：它只在 T3（本地量化）时给的是读数而不是画面，会顶掉 OpenAI / 视觉复核模型槽里坐着的真视觉模型。现在只有 T3 的 agent 会排到真视觉模型之后（仍留在链上做兜底）。

## [v1.8.0] - 2026-08-16

### 移除

- **摘掉 GBrain**。知识库已经归 IvyeaAgent，GBrain 留着只会误导：
  - 设置页那个「知识库语义检索（Embedding）」三个键只写进 `~/.gbrain/config.json`，对 IvyeaAgent 的 `retrieval_embedding_*` 一个字都没动 —— 认真填完 key，agent 侧仍是默认后端，还不报错。该配置项已移除；**语义检索现在由 IvyeaAgent 自带，装完即用，不需要在这里配任何东西**（需 IvyeaAgent v1.12.0+）。
  - 监控不再把 `gbrain` / `gbrain serve` 当必检进程 —— 知识库跑在 agent 里，探不到就报「知识库功能不可用」是必然误报。
  - 安装器（`setup.sh` / `install.sh` / `install-components.ps1` / 面板一键安装）不再拉 bun + GBrain，也不再交互式问「要不要下 400MB 的 Ollama 做语义检索」。新装少 400MB 下载和一轮提问。
  - 面板加载时不再自动 `gbrain init --pglite` + 自动接 Ollama embedding（新机器上等于自动初始化一个用不到的库，只会抛一堆无关的错），改成只读判断。
  - `/brain` 对话里 GBrain 检索失败不再伪装成一条「引用」显示「检索失败：...」—— 回答本身来自 IvyeaAgent，不依赖这条路，改为只记日志。

  未动：`BRAIN_ROOT` 下的文件读写、`/brain` 的 overview/stats/page 仍走旧路径 —— 那是真正的迁移（要连带迁「点引用打开原文」），另开改动做。

## [v1.7.0] - 2026-08-15

### 变更

- **「静谧」主题 + 外壳收纳**：把界面从描边堆里拆出来。

## [v1.6.0] - 2026-08-14

### 变更

- **任务台的执行过程改成一行**。此前一轮里的每一步都铺开显示，192 步的任务能把整页糊满、把回答挤出屏幕。现在页面上常驻的只有**当前正在执行或思考的那一行**，新一步到达就顶掉上一步；想看全过程点右边的「展开」，一步一行、带时刻和耗时，面板内部滚动，不会再把页面撑长。收起它的开关也从行尾一个几乎看不见的小箭头换成了带文字的按钮。
- **能看到模型在想什么了**。会思考的模型（本机的 deepseek-v4-pro 就是）在调工具之间的思考流现在会显示在活动行上。此前网页端从来收不到这个 —— agent 一直只把它给了终端。
- **输入框下方新增一条会话统计**：几轮几步、用时、其中纯模型时间是多少（剩下的就是工具花掉的）、首字延迟、输出速度、缓存命中率、输入输出 token。哪一项测不到就显示「—」，不会拿 0 充数。

### 修复

- **刷新后再打开历史会话，不会再少掉你发过的指令**。此前会话详情固定只回最后 30 条消息，而一次提问会产生几十条消息（工具调用占大头），你自己打的那句话就被挤出了窗口 —— 本机一条 413 条消息、15 次提问的会话，刷新后只剩 1 次提问看得见，另一条 6 次提问的会话一条都不剩。现在按**轮**分页，默认给最近 8 轮，线程顶部有「加载更早的对话」。AI 问答与知识库对话吃的是同一个亏，一并修了。
- **刷新后执行过程也回得来了**。步骤此前只活在直播的事件流里，关掉页面就没了；现在它们和消息一起落盘，重新打开会话时按 call_id 挂回各自那一轮，工具名、状态、耗时和当时看到的一致。改动之前的老会话没有这份记录，打开时执行过程为空（不会伪造）。

### 新增

- 需要 IvyeaAgent v1.10.3 才有「模型思考」「纯模型时间」和「刷新后仍在的执行过程」；连着旧版 agent 时其余功能照常，这几项自动隐藏。
- **思考流不落盘**：它只在生成过程中可见，刷新后不会回来（一次普通提问就有几十段思考，存下来只会把会话文件撑大）。执行过程不受影响。

## [v1.5.3] - 2026-08-14

长任务流式输出下的两个硬伤，都在「你盯着屏幕等结果」的那几分钟里。

### 修复

- **同一段内容不再反复显示**。一轮里正文本来就会被吐好几遍（工具调用前的开场白、门禁打回后的整篇重写），此前前端把它们全拼进同一个气泡，看上去就是复读。现在按 agent 给出的边界处理：整篇重写则旧稿作废（时间线上留一行说明它为什么被推翻），只是说到一半则断段不丢字。任务台、AI 问答、悬浮球、知识库四处都改了。
- **输出没结束时也能往上翻了**。此前一滑就被弹回底部——判据是"离底部多远"，而滚动事件是异步的、token 是同步的，用户翻上去的那一下永远被下一个 token 抢先拍回去。现在按用户意图判：一旦你自己滚动就停止跟随，直到你真的回到底部或点「回到底部」。任务台补了「回到底部」药丸。

长回答的滚动与重绘也顺带变流畅了（此前每个 token 都会把整轮 markdown 重新解析一遍）。

配套修复见 IvyeaAgent v1.10.2 —— 那边是根治重复的源头，这边是消费侧。

## [v1.4.0] - 2026-08-09

这一批全部落在**你每天看得见的地方**：花费、审批、证据、导出、第一次打开的那五分钟。

### 新增

- **顶栏常驻「本月已花 $X」**。AI 花费是这个产品里唯一会在你不看的时候持续增长的东西 —— 放在设置页里，只有起疑心时才会去翻；放在顶栏，异常增长当天就能发现。悬停能看到预算用量和这个数是几分钟前算的。
- **AI 月度预算分两档**：用到 80% 提醒一次，到 100% 再提醒一次并**暂停自动任务**。手动操作照跑不误 —— 你明确要的东西不该被一个本地估算数按住，停的只是"没人盯着也会自己跑"的定时任务。被跳过的定时任务会在它自己的历史里写明原因，不会让你以为坏了。
- **待审批页（手机上能点）**。Agent 要动真实数据时会停下来问你，现在所有等待确认的操作汇总在一页，手机上点同意/拒绝就行，不必先找到是哪个会话。配了通知渠道的话还会先响一声。这是"只审批、不下达"——从手机上没法凭空发起一个新指令，少了一整类误操作。
- **证据可以再点进一层，看它依据的原始数据行**。此前证据只给到「指标 / 数值 / 时间窗 / 来源」，现在能直接翻到源报表里的那几行，原样显示、不做任何换算。找不到时会明说"没检索到"，而不是显示成"这条没有证据"——两件完全不同的事。
- **周报 PPTX 导出**：每条结论一页，证据就在同一页上（会上被问到"凭什么"时，翻页找附录已经晚了）。**PDF 导出**：服务器上有 Chrome 就能用，没有会告诉你从 HTML 报告里 Ctrl+P，效果一样。
- **首启向导加了「试跑一次」**。填完配置直接落在空首页上，是配置向导最常见的失败。现在离开向导之前会用本地知识库跑一次真实检索——不花钱、不联网、几百毫秒出结果，每条都带官方出处。密钥那一步也补上了「我没有 API Key 怎么办」，给的是三条真出路（先跳过 / 用 Ollama / 去买一个）。
- **领星广告规则引擎接上了统一结论契约**：日常走领星的那条链路现在也有结论卡片和带证据页的交付物导出，而且它的证据比 AI 那条更硬——是接口取回的真实指标，不是模型转述。

### 修复

- **开启能力市场后整页崩溃**。参数传错触发 422，而 FastAPI 的 422 里 `detail` 是对象数组，被直接当文案渲染——整页崩成「渲染失败」，真正的原因一个字都看不到。全站 99 处同类写法一并改掉，并加了门禁防止回潮。
- **能力市场里看不到门道社区的技能**。社区那一路此前只做在「Skill 中心」的子页面里，而「能力市场」板块从头到尾没有它。现在补上了，开关也直接放在这一页——之前藏在系统配置里太深。
- **Windows 上装不了任何社区技能**。安装路径的包含检查写死了正斜杠，在 Windows 上恒不成立，点安装永远得到"非法的安装路径"。同一处还有个同名前缀的漏洞（`community-evil` 会被判成在 `community` 之内），一并堵掉。
- **开了 include_media 的备份会把历史备份和自己装进去**，体积滚雪球且会嵌入半成品。

### 内部

- 花费聚合本机实测 8.81 秒，顶栏不能直接接——加了缓存层后降到 19ms。
- 新增评测案例脱敏工具：一条命令把真实任务变成脱敏案例骨架（ASIN 化名、金额按同系数缩放且比值守恒）。合格要点仍必须人工标注。

## [v1.3.0] - 2026-08-09

这一批的主题是"把自用工具做成能托付的产品"：出了问题查得到、数据丢不了、多人用时权限收得住；
再加上三件让它能被**别人**用起来的事 —— 技能来源、对外接口、和能发出去的交付物。

### 安全

- **子进程不再能读到会话签名密钥。** 此前 `server/.env` 被 `load_dotenv()` 灌进进程环境，于是**每个**子进程（终端里敲的命令、AI 决定的工具调用、Skill 脚本、外部 MCP server）都能直接读到 `IVYEA_OPS_SECRET` —— 拿到它就能伪造管理员会话。现在启动时就把凭据从环境里摘走，高危路径的子进程再单独剥一层。
- **配置里的 API 密钥在磁盘上加密了**（AES-256-GCM）。老装机的明文配置照常能用，下次保存时自动转成密文。
  > ⚠️ **换机器请用「带口令」的备份包**：解密要用 `data/.master.key`，这是个隐藏文件，手动拷 `data/` 极易漏掉。
  > 需要回退到旧版本时，先跑 `python -m app.core.secrets --decrypt-all` 把盘上还原成明文，否则旧版会把密文当成 key 去调 API，表现为"所有集成突然全部鉴权失败"。
- **智能体板块的越权收紧了。** 被授予该板块的普通成员不再能读项目目录以外的文件、浏览整个文件系统、在任意位置建项目 —— 这些仍归管理员。管理员的"上一级"浏览不受影响。

### 新增

- **能力市场接到门道社区**。可以浏览、查看能力清单、安装社区分享的 Skill。**默认关闭** —— 它会往外发请求，而这个产品的默认立场是数据不出你的机器；开启后也只在你主动浏览或安装时联网，请求匿名、不带机器标识。安装前先给你看这个 Skill 会读什么、跑什么、连哪里，确认后才落盘；安装包校验 sha256 与 Ed25519 签名。
- **对外 MCP：让别的 AI 用上你的数据**（系统配置 → 对外 MCP）。生成一个令牌，Claude Desktop、Cursor 或任何支持 MCP 的客户端就能调用这台机器上的广告结论、知识库和关键词调研。**令牌指向的是你自己的服务器** —— 数据不经过我们，也不经过任何第三方云。令牌只存哈希、可设有效期、可随时撤销，读写权限分开且写默认不给。设置页直接给出可粘贴的客户端配置片段。
- **通知渠道**：任务失败、AI 花费超预算、有操作等你确认，直接推到手机上。粘一个飞书 / 钉钉 / 企业微信 / Slack 的 webhook 就行 —— **是哪一家我们自己认**，不用你选。默认只发需要你动手的几类，不发"任务完成"。报文里只有事件类型、任务名和时间，不含任何店铺数据或密钥。
- **AI 月度预算**：填一个金额，超了推一条通知，**每月只提醒一次**。它**不会**掐掉你正在跑的任务 —— 这是按公开价目表对 token 的本地估算，不是账单。
- **带证据页的交付物导出**（广告审计 → 「交付物」/「结论摘要」）：三页 Excel —— 结论、证据、说明。每条结论后面跟着指标、数值、时间窗和数据来源，别人质疑"你凭什么这么说"时翻到第二页。不可回滚的动作单独标黄。另有同内容的 Markdown 版，可直接贴进飞书文档或 Notion。
- **结论有了统一格式**。各板块的 AI 结论现在都带结构化证据（指标 / 数值 / 时间窗 / 来源）和带阈值的建议动作。**没有证据支撑的结论会被显式标出来**，而不是混在里面看起来一样可信。
- **AI 输出质量评测框架**：五个维度打分，事实性错误一票否决。开放生态之前先有个尺子。
- **换机器搬家脚本**，并且"跨平台能迁"这句话现在由 CI 每次验证（Linux ↔ Windows 双向）。
- **一键诊断包**（系统配置页 / `GET /api/health/diagnostic-bundle`）：版本、日志尾部、脱敏配置、各库表行数打成一个 zip，报问题时直接附上。**不含密钥、不含店铺数据，导出过程不发起任何网络请求。**
- **备份 / 校验 / 恢复**。429MB 的数据目录约压成 30MB（数据库走 SQLite 在线快照，不停服；图片日志等可再生内容默认不收）。恢复默认只**干跑**，先告诉你会覆盖什么，确认后才执行。每天凌晨 3 点自动备份一次，本地保留 7 份。
- **统一审计流水**（`GET /api/audit`，管理员，支持 CSV 导出）：谁改了配置、谁触发了自动修复、谁停了服务、谁动了 MCP 配置。
- **任务账本**（`GET /api/admin/jobs`）：长任务不再因为重启而凭空消失。被打断的任务标为"孤儿"并显示在列表里 —— 和"执行失败"是两回事。改动过外部系统的任务**不会**自动重跑。
- **报错能查了**：每个请求带 `request_id`，出现在错误提示和日志里；日志默认落盘到 `data/logs/`（10MB × 5 轮转）。错误提示现在会告诉你下一步该做什么。

### 修复

- 智能体板块的**权限依赖绕过了测试替身**，导致 `server/app/tests` 里 61 项测试常年失败而无人知晓（CI 当时不跑那批）。现已修复并纳入 CI。
- Listing 分析解析模型回复时用 `strip("```json")` —— 那是按**字符集**剥离而不是去前缀，遇到"带说明文字包裹的围栏 JSON"（最常见的回复形态）直接解析失败。
- 删除一个内部死函数（调用即 `NameError`）；补上两处漏掉的类型导入（一旦变成接口依赖就会当场报错）。

### 内部

- 后端接入 `ruff` 门禁并进 CI；测试矩阵补上 macOS（此前只打包不测试）。
- 新增失败分类与自动恢复层：网络抖动会重试，**密钥过期和余额不足立刻停** —— 重试一百次也不会成功，只会烧掉你自己的 API 额度。
- **智能体页打开快了一倍**：首屏需要下载的 JS 从 2362 kB 降到 1189 kB（gzip 675 → 361）。终端、文件编辑器、数学公式、Git 面板、设置页等改为用到才加载。
- 清掉 186 处"静默吞掉异常"的写法 —— 那是"功能时好时坏又说不清原因"的结构性根因；ruff 门禁已把这条规则收进去，不许回潮。

---

## [v1.2.0] - 2026-08-08

见 [Releases](https://github.com/Hector-xue/IvyeaOps/releases) 页的发行说明。

## [v1.1.116] - 2026-08-06

自动链路全面改用 IvyeaAgent（此前是 hermes）。

---

更早的版本请看 [Releases](https://github.com/Hector-xue/IvyeaOps/releases)。
