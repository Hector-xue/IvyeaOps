// 顶栏右侧的板块动作挂位。
//
// 顶栏瘦身之后只剩"我在哪儿"这一行字。但有些板块确实有一个**属于这一页的主
// 动作**（任务台的「新建任务」是最明显的一个），它不该被塞进侧栏的账户菜单
// —— 那里装的是全局设置，和"我现在这一页要干什么"是两码事。
//
// 所以留一个挂位：板块用 <TopbarActions> 往里 portal 内容，没有板块挂东西时
// 这块**不占任何位置**（不渲染占位符、不留空白按钮）。顶栏保持空的，是它本来
// 就该有的样子。

/** 顶栏挂位的 DOM id。MainLayout 渲染它，TopbarActions 往里 portal。 */
export const TOPBAR_SLOT_ID = "ivyea-topbar-slot";
