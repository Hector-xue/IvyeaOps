/**
 * 常春藤生长 —— Agent 正在思考时的那枚小图标。
 *
 * 换掉的是一个紫色的星号在原地打转。转圈是"进度未知"的通用符号，它跟这个产品没有
 * 任何关系，紫色更是从别处借来的；而 Ivyea 本来就是常春藤（品牌绿 #5B9334，官网
 * 那条"移动鼠标让常春藤生长"的动效用的就是它）。所以：**思考 = 藤在长**。
 *
 * 动画是"画出来"的，不是转出来的：藤蔓沿着自己的路径生长（stroke-dashoffset），
 * 三片叶子按先后冒出来，长满后整体轻轻褪一下再从头长 —— 一个 2.8s 的循环。
 * 循环意味着"还在继续"，而每一轮都在往前长，意味着"它在推进"，这两件事恰好就是
 * 用户盯着这枚图标时想知道的。
 *
 * 尺寸跟着字号走（em），所以它在活动行、气泡里、窄屏下都不用各写一份大小。
 * 动效全在 workbench.css 的 .ivy-grow 里 —— 这里只有形状。
 */
export default function IvyGrow({ title = "正在思考" }: { title?: string }) {
  return (
    <svg className="ivy-grow" viewBox="0 0 24 24" role="img" aria-label={title}>
      <title>{title}</title>
      {/* 主藤：从底部起，一条向上偏右再回摆的 S 形 —— 真实的藤是绕着长的，
          一条直线读起来像进度条，不像植物。 */}
      <path className="ivy-stem"
            d="M12 22 C 12 18, 8.5 16.5, 9 12.5 C 9.4 9, 13 8, 13.5 4.5"
            fill="none" strokeLinecap="round" />
      {/* 三片叶子，沿藤从下往上。叶子是两段对称曲线合成的水滴形，
          分别挂在左、右、左 —— 交替生长才像藤，全挂一侧像羽毛。 */}
      <path className="ivy-leaf ivy-leaf-1"
            d="M10.6 17.4 C 8.4 17.8, 6.8 16.6, 6.6 14.8 C 8.8 14.4, 10.4 15.6, 10.6 17.4 Z" />
      <path className="ivy-leaf ivy-leaf-2"
            d="M10.2 12.2 C 12.4 11.4, 14.2 12.2, 14.8 14 C 12.6 14.8, 10.8 14, 10.2 12.2 Z" />
      <path className="ivy-leaf ivy-leaf-3"
            d="M12.4 7.6 C 10.4 6.8, 9.6 5.2, 10.4 3.4 C 12.4 4.2, 13.2 5.8, 12.4 7.6 Z" />
    </svg>
  );
}
